export enum Paths {
  individuals = 'individuals',
  resources = 'resources',
  fullUrl = 'resources/individuals',
}

export enum BadFiles {
  dsStore = '.DS_Store',
}

export enum FileType {
  Video = 'video',
  Photo = 'photo',
  Artifact = 'artifact',
}
